We analyze US gun murder dats collected by the FBI

download-data.R  - Download csv file to data directory

wrangle-data.R   - Creates a derived dataset and saves as Robject in rdas directory

analysis.R   - a plot is generated and saved in the figs directory